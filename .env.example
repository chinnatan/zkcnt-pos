# PocketBase URL — browser must reach this address (baked into frontend at build time)
# Local dev: http://localhost:8090
# Pi production: http://<pi-ip-or-domain>  (e.g. http://192.168.1.50)
POCKETBASE_URL=http://localhost:8090

# Production Nginx port
PORT=80
